# NeuralGrid — Smart Building Production Server
# Install: pip install -r requirements.txt

# Web frameworks
flask>=3.1.2
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Production WSGI server (Windows compatible)
waitress>=3.0.0

# Flask middleware
flask-limiter>=3.5.0
flask-compress>=1.15

# Input validation
pydantic>=2.6.0

# Data & ML
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0

# Security
cryptography>=42.0.0
